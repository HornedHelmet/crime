#pragma once
namespace algorithm_aoc
{
    template<typename T>
    class NumericAlgorithm
    {
    public:
        static T Clamp(T val, T max, T min);
    };

    // Add more classes here as needed
    //
    //
}